# Create the requirements file (requirements.txt) add the required modules

streamlit
python-dotenv
langchain-community
langchain-groq

# Terminal run the command - (-r :- requares in installation)

pip install -r requirements.txt

# Create .env file
 
GROQ_API_KEY=your_groq_api_key

# Create the python file chatbot.py

add the code this file

# Run the project

streamlit run chabot.py

# Browser

localhost:8052





